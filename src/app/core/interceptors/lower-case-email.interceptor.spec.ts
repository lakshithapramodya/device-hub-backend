import { LowerCaseEmailInterceptor } from './lower-case-email.interceptor';

describe('LowerCaseEmailInterceptor', () => {
  it('should be defined', () => {
    expect(new LowerCaseEmailInterceptor()).toBeDefined();
  });
});
